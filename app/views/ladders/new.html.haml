%div.container-large-offset.centeredContainer.text-center
  %h1.titleize New Ladder
  %div.small-content-wrapper.centeredContainer
    - if alert
      .alert.alert-danger.alert-dismissable.text-left= alert
    = render 'form'
