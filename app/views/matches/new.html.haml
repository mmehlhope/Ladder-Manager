- content_for(:title, "Record a new match")
%div.col-small.centeredContainer.container-large-offset
  %h1.panelHeading= yield(:title)
  .panel.panel-full-padding
    = render 'includes/message' if flash_message?
    = render 'create'
