- content_for(:title, "#{@organization.name}")
%section.main
  %header.clearfix
    .section-title
      %h1= yield(:title)

  = render 'includes/message'

  %section.organization-panel
    %ul.navigation.three-across
      %li.active
        %a.btn.btn-link(href="#ladders" data-toggle="tab" title="Edit Ladders") Ladders
      %li
        %a.btn.btn-link(href="#users" data-toggle="tab" title="Edit Users") Users

    .tab-content

      .tab-pane#ladders.active

      .tab-pane#users

:javascript
  require(['backbone/pages/manage_organization'], function(ManageOrgPage) {
    new ManageOrgPage({
      organization: #{@organization_json},
      ladders: #{@ladders_json},
      users: #{@users_json}
    });
  });
