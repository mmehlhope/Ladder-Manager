# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below

match_one:
  competitor_1: <%= ActiveRecord::FixtureSet.identify(:competitor_one) %>
  competitor_2: <%= ActiveRecord::FixtureSet.identify(:competitor_two) %>
  finalized: false
  ladder: ladder_one
  winner_id: NULL

match_two:
  competitor_1: <%= ActiveRecord::FixtureSet.identify(:competitor_three) %>
  competitor_2: <%= ActiveRecord::FixtureSet.identify(:competitor_four) %>
  winner_id: NULL
  ladder: ladder_one
  finalized: false